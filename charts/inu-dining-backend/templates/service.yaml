apiVersion: v1
kind: Service
metadata:
  name: {{ include "Dining-backend.fullname" . }}
  labels:
    {{- include "Dining-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Dining-backend.selectorLabels" . | nindent 4 }}
