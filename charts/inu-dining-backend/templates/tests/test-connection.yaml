apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Dining-backend.fullname" . }}-test-connection"
  labels:
    {{- include "Dining-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Dining-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
